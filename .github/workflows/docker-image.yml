name: Docker Image CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
      -
        name: Checkout
        uses: actions/checkout@v2
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v1
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      -
        name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERACR_USERNAME }}
          password: ${{ secrets.DOCKERACR_TOKEN }}
      -
        name: Build and push
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          tags: acrcomunidadecloud.azurecr.io/mycontainer:$(date +%s)
    
    
    #- uses: actions/checkout@v3
    #- name: Build the Docker image
    #  run: docker build . --file Dockerfile --tag acrcomunidadecloud.azurecr.io/mycontainer:$(date +%s)
     
    #- name: Push the docker image
    #  run: docker push acrcomunidadecloud.azurecr.io/myconmtainer:$(date +%s)
    

